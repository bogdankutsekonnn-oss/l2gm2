# Руководство по изменению отступов

## 📏 Где изменить отступы от header до H1

### Вариант 1: Глобальный стиль (рекомендуется)

**Файл:** `assets/css/main.css`

Здесь определены глобальные стили для `.page-header`, которые применяются ко всем страницам:

```css
.page-header {
  padding-top: calc(var(--spacing-xxl) * 2); /* Отступ сверху (от header) */
  margin-bottom: calc(var(--spacing-xl) * 2); /* Отступ снизу */
}
```

**Текущие значения:**
- `--spacing-xxl` = 48px
- `--spacing-xl` = 32px
- Увеличены в 2 раза: 96px и 64px соответственно

**Чтобы изменить:**
1. Откройте `assets/css/main.css`
2. Измените множитель (например, `* 2` на `* 3` для увеличения в 3 раза)
3. Или укажите конкретное значение: `padding-top: 120px;`

### Вариант 2: Изменить в конкретной странице

Если нужно изменить отступы только на одной странице, откройте файл страницы (например, `pages/index.vue`) и найдите:

```css
.page-header {
  margin-bottom: var(--spacing-xl);
}
```

Измените на:
```css
.page-header {
  padding-top: calc(var(--spacing-xxl) * 2); /* Отступ сверху */
  margin-bottom: calc(var(--spacing-xl) * 2); /* Отступ снизу */
}
```

### Вариант 3: Изменить CSS переменные

**Файл:** `layouts/default.vue` (строки 48-53)

Измените значения переменных:

```css
:root {
  --spacing-xl: 64px;   /* Было 32px, теперь 64px (x2) */
  --spacing-xxl: 96px;  /* Было 48px, теперь 96px (x2) */
}
```

⚠️ **Внимание:** Это изменит все отступы на сайте, где используются эти переменные!

## 🎯 Текущие отступы

### От header до H1

**Глобальный стиль в `assets/css/main.css`:**
- `padding-top: calc(var(--spacing-xxl) * 2)` = **96px** (было 48px)

### От H1 до контента

**Глобальный стиль в `assets/css/main.css`:**
- `margin-bottom: calc(var(--spacing-xl) * 2)` = **64px** (было 32px)

### От баннера до контента

**В `layouts/default.vue`:**
- `padding-top: calc(var(--spacing-xxl) * 2)` = **96px** для `.main-content`

## 📝 Быстрое изменение

### Увеличить в 3 раза вместо 2:

В `assets/css/main.css`:
```css
.page-header {
  padding-top: calc(var(--spacing-xxl) * 3); /* 144px */
  margin-bottom: calc(var(--spacing-xl) * 3); /* 96px */
}
```

### Установить конкретное значение:

```css
.page-header {
  padding-top: 120px;  /* Конкретное значение */
  margin-bottom: 80px;
}
```

### Использовать другую переменную:

```css
.page-header {
  padding-top: var(--spacing-xxl);  /* 48px */
  margin-bottom: var(--spacing-xl); /* 32px */
}
```

## 🔍 Где определены стили на страницах

Отступы для `.page-header` определены в:

1. **Глобально:** `assets/css/main.css` (применяется ко всем страницам)
2. **Локально:** В каждом файле страницы в секции `<style scoped>`

Если на странице есть локальный стиль `.page-header`, он переопределит глобальный.

## 💡 Рекомендации

1. **Используйте глобальный стиль** в `assets/css/main.css` для единообразия
2. **Используйте CSS переменные** для легкого изменения всех отступов
3. **Используйте `calc()`** для математических операций с переменными

## 📐 Текущие значения переменных

Из `layouts/default.vue`:
- `--spacing-xs: 4px`
- `--spacing-sm: 8px`
- `--spacing-md: 16px`
- `--spacing-lg: 24px`
- `--spacing-xl: 32px`
- `--spacing-xxl: 48px`
