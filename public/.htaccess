# Trailing slash redirect — fixes canonical duplicates in Google
RewriteEngine On

# If the request is not for a file and doesn't end with a slash — redirect 301
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_URI} !(.*)/$
RewriteRule ^(.*)$ /$1/ [R=301,L]
