# Руководство по замене статусных иконок (VIP, TOP, PREM)

## 🎯 Где находятся статусные лейблы

Статусные лейблы (VIP, TOP, PREM) находятся в компоненте **`components/ServerCard.vue`** в функции `getStatusIcon()`.

## 📁 Структура папок для SVG

Создайте следующую структуру:

```
public/
└── images/
    └── status/
        ├── premium.svg  (или premium.png)
        ├── vip.svg      (или vip.png)
        └── top.svg      (или top.png)
```

## 🔧 Как заменить на SVG

### Шаг 1: Создайте папку и поместите файлы

1. Создайте папку `public/images/status/` в корне проекта
2. Поместите туда ваши SVG файлы:
   - `premium.svg` - для статуса PREM
   - `vip.svg` - для статуса VIP
   - `top.svg` - для статуса TOP

### Шаг 2: Проверьте компонент

Компонент уже настроен на использование изображений из этой папки. Функция `getStatusIcon()` автоматически загружает:
- `/images/status/premium.svg` для статуса `premium`
- `/images/status/vip.svg` для статуса `vip`
- `/images/status/top.svg` для статуса `top`

### Шаг 3: Готово!

После размещения файлов иконки автоматически отобразятся вместо текста.

## 📐 Требования к изображениям

- **Размер:** 24x24px (рекомендуется)
- **Формат:** SVG (предпочтительно) или PNG
- **Прозрачность:** Поддерживается
- **Цвет:** Может быть любой, но лучше использовать цвета, которые хорошо видны на фоне статуса

## 🎨 Текущие цвета фона статусов

В компоненте определены цвета фона для каждого статуса:

```css
.status-premium {
  background: var(--primary-main);  /* Красный */
  color: var(--primary-contrast);
}

.status-vip {
  background: var(--status-warning);  /* Желтый */
  color: var(--text-primary);
}

.status-top {
  background: var(--status-success);  /* Зеленый */
  color: var(--text-primary);
}
```

Учитывайте эти цвета при создании иконок.

## 🔄 Альтернативные варианты

### Вариант 1: Использовать PNG вместо SVG

Просто замените расширение файлов на `.png`:
- `premium.png`
- `vip.png`
- `top.png`

Компонент автоматически подхватит PNG файлы.

### Вариант 2: Встроенные SVG

Если хотите использовать встроенные SVG вместо файлов, откройте `components/ServerCard.vue` и найдите функцию `getStatusIcon()`. Раскомментируйте вариант с встроенными SVG и замените пути:

```javascript
const getStatusIcon = (status) => {
  const iconMap = {
    'premium': () => h('svg', { width: 24, height: 24, viewBox: '0 0 24 24' }, [
      h('path', { d: 'ВАШ_SVG_PATH', fill: 'currentColor' })
    ]),
    'vip': () => h('svg', { width: 24, height: 24, viewBox: '0 0 24 24' }, [
      h('path', { d: 'ВАШ_SVG_PATH', fill: 'currentColor' })
    ]),
    'top': () => h('svg', { width: 24, height: 24, viewBox: '0 0 24 24' }, [
      h('path', { d: 'ВАШ_SVG_PATH', fill: 'currentColor' })
    ])
  }
  return iconMap[status] || (() => h('div', statusText.value))
}
```

### Вариант 3: Разные размеры для разных статусов

Если нужно использовать разные размеры, измените функцию:

```javascript
const getStatusIcon = (status) => {
  const sizeMap = {
    'premium': { width: 24, height: 24 },
    'vip': { width: 24, height: 24 },
    'top': { width: 24, height: 24 }
  }
  
  const size = sizeMap[status] || { width: 24, height: 24 }
  
  return h('img', {
    src: `/images/status/${status}.svg`,
    alt: statusText.value,
    ...size,
    style: 'display: block; object-fit: contain;'
  })
}
```

## ✅ Быстрая проверка

После размещения файлов:

1. Убедитесь, что файлы находятся в `public/images/status/`
2. Проверьте имена файлов (должны быть: `premium.svg`, `vip.svg`, `top.svg`)
3. Перезапустите dev сервер: `npm run dev`
4. Проверьте в браузере, что иконки отображаются

## 🔍 Отладка

Если иконки не отображаются:

1. **Проверьте путь в браузере:**
   Откройте `http://localhost:3000/images/status/premium.svg` - файл должен открыться

2. **Проверьте консоль браузера:**
   Откройте DevTools (F12) → Console, ищите ошибки 404

3. **Проверьте имена файлов:**
   - Должны быть точно: `premium.svg`, `vip.svg`, `top.svg`
   - Регистр важен на некоторых серверах

4. **Проверьте формат:**
   - SVG файлы должны быть валидными
   - PNG файлы должны иметь правильное расширение

## 💡 Рекомендации

1. **Используйте SVG** для лучшего качества и масштабирования
2. **Размер 24x24px** соответствует размеру контейнера
3. **Оптимизируйте SVG** перед использованием (удалите лишние метаданные)
4. **Тестируйте на разных фонах** - статусы имеют разные цвета фона

## 📝 Пример структуры SVG

Минимальный SVG файл должен выглядеть так:

```svg
<svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
  <path d="ВАШ_ПУТЬ" fill="currentColor"/>
</svg>
```

Или с конкретным цветом:

```svg
<svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
  <path d="ВАШ_ПУТЬ" fill="#FFFFFF"/>
</svg>
```

## 🚀 Готово!

После размещения файлов в `public/images/status/` иконки автоматически заменят текстовые лейблы VIP, TOP, PREM.
