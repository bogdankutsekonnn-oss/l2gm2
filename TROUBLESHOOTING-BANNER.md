# Решение проблем с баннером

## 🔍 Проверка проблемы

### 1. Проверьте структуру папок

Убедитесь, что папка `public` существует в корне проекта:

```
l2gm2/
├── public/          ← Эта папка должна существовать
│   └── images/
│       └── banner.jpg
├── components/
├── pages/
└── ...
```

### 2. Проверьте путь к файлу

В Nuxt 3 файлы из папки `public/` доступны по корневому пути:

- ✅ Правильно: `/images/banner.jpg`
- ❌ Неправильно: `~/public/images/banner.jpg`
- ❌ Неправильно: `public/images/banner.jpg`

### 3. Проверьте имя файла

Убедитесь, что имя файла точно совпадает (регистр важен на некоторых серверах):
- `banner.jpg` ✅
- `Banner.jpg` ❌ (может не работать)
- `banner.JPG` ❌ (может не работать)

### 4. Проверьте формат файла

Поддерживаемые форматы:
- `.jpg` / `.jpeg`
- `.png`
- `.webp`
- `.gif`

## 🛠️ Решения

### Решение 1: Использовать абсолютный путь

Откройте `components/TopBanner.vue` и измените:

```vue
<script setup>
// Попробуйте разные варианты пути
const bannerImage = ref('/images/banner.jpg')

// Или попробуйте без начального слеша
// const bannerImage = ref('images/banner.jpg')

// Или полный URL (если изображение на другом сервере)
// const bannerImage = ref('https://example.com/banner.jpg')
</script>
```

### Решение 2: Проверить в браузере

1. Откройте DevTools (F12)
2. Перейдите на вкладку Network
3. Обновите страницу
4. Найдите запрос к `banner.jpg`
5. Проверьте статус:
   - **200** - файл найден, проблема в отображении
   - **404** - файл не найден, проверьте путь
   - **403** - нет доступа к файлу

### Решение 3: Использовать require (для статических файлов)

Если изображение в `assets/`:

```vue
<script setup>
const bannerImage = ref(new URL('~/assets/images/banner.jpg', import.meta.url).href)
</script>
```

### Решение 4: Временное решение - внешняя ссылка

Для тестирования используйте внешнюю ссылку:

```vue
<script setup>
const bannerImage = ref('https://via.placeholder.com/1920x600/DF2027/F5F5F7?text=Banner+1920x600')
</script>
```

## 🔧 Отладка

Добавьте в компонент для отладки:

```vue
<template>
  <div class="top-banner">
    <!-- Временно показываем путь для отладки -->
    <div style="position: absolute; top: 0; left: 0; z-index: 9999; background: red; color: white; padding: 10px;">
      Путь: {{ bannerImage }}
    </div>
    <img
      v-if="bannerImage"
      :src="bannerImage"
      alt="Рекламный баннер"
      class="banner-image"
      @error="handleImageError"
      @load="handleImageLoad"
    />
    <div v-else class="banner-placeholder">
      <span>Рекламный баннер</span>
      <span class="banner-size">1920x600px</span>
    </div>
  </div>
</template>

<script setup>
const bannerImage = ref('/images/banner.jpg')

const handleImageError = (event) => {
  console.error('Ошибка загрузки баннера:', event.target.src)
  bannerImage.value = null
}

const handleImageLoad = () => {
  console.log('Баннер успешно загружен!')
}
</script>
```

## ✅ Чек-лист

- [ ] Папка `public/images/` существует
- [ ] Файл `banner.jpg` находится в `public/images/`
- [ ] Имя файла написано правильно (без заглавных букв)
- [ ] Формат файла поддерживается (.jpg, .png, .webp)
- [ ] Размер файла не слишком большой (< 5MB рекомендуется)
- [ ] В консоли браузера нет ошибок 404
- [ ] Сервер разработки перезапущен после добавления файла

## 🚀 Быстрое решение

1. Создайте папку `public/images/` в корне проекта
2. Поместите туда файл `banner.jpg`
3. Перезапустите dev сервер: `npm run dev`
4. Откройте в браузере: `http://localhost:3000/images/banner.jpg`
   - Если изображение открывается - путь правильный
   - Если 404 - проверьте структуру папок

## 💡 Альтернатива

Если ничего не помогает, используйте base64:

```vue
<script setup>
// Временно для тестирования
const bannerImage = ref('data:image/svg+xml;base64,...')
</script>
```

Или используйте компонент `<NuxtImg>` из Nuxt Image:

```vue
<template>
  <div class="top-banner">
    <NuxtImg
      src="/images/banner.jpg"
      alt="Рекламный баннер"
      class="banner-image"
      format="webp"
    />
  </div>
</template>
```
