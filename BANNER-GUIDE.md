# Руководство по замене баннера

## 📁 Где разместить изображение баннера

В Nuxt 3 есть два способа размещения изображений:

### Вариант 1: Папка `public/` (рекомендуется)

Создайте папку `public/images/` и поместите туда изображение:

```
l2gm2/
└── public/
    └── images/
        └── banner.jpg  (или banner.png, banner.webp)
```

**Преимущества:**
- Изображение доступно напрямую по URL: `/images/banner.jpg`
- Не обрабатывается сборщиком
- Можно легко заменить без пересборки

### Вариант 2: Папка `assets/`

```
l2gm2/
└── assets/
    └── images/
        └── banner.jpg
```

**Использование:**
```vue
<script setup>
const bannerImage = new URL('~/assets/images/banner.jpg', import.meta.url).href
</script>
```

## 🖼️ Требования к изображению

Согласно документации:
- **Размер:** 1920x600px
- **Пропорции:** 16:5 (1920/600 = 3.2)
- **Формат:** JPG, PNG или WebP

## 🔧 Как заменить баннер

### Шаг 1: Подготовьте изображение

1. Создайте изображение размером **1920x600px**
2. Сохраните в формате JPG, PNG или WebP
3. Назовите файл, например: `banner.jpg`

### Шаг 2: Разместите файл

**Создайте папку и поместите файл:**

```bash
# В корне проекта создайте:
public/
└── images/
    └── banner.jpg
```

Или создайте вручную:
- Создайте папку `public` в корне проекта
- Внутри создайте папку `images`
- Поместите туда ваше изображение `banner.jpg`

### Шаг 3: Обновите компонент (уже сделано)

Компонент `TopBanner.vue` уже настроен на использование изображения из `/images/banner.jpg`

Если вы хотите использовать другое имя файла или путь, откройте `components/TopBanner.vue` и измените:

```vue
<script setup>
// Измените путь на ваш файл
const bannerImage = '/images/ваш-файл.jpg'
</script>
```

## 📝 Примеры использования

### Использование внешней ссылки

```vue
<script setup>
const bannerImage = 'https://example.com/banner.jpg'
</script>
```

### Условное отображение (с fallback)

```vue
<script setup>
// Если изображение не загрузится, покажется placeholder
const bannerImage = '/images/banner.jpg'
</script>

<template>
  <div class="top-banner">
    <img
      v-if="bannerImage"
      :src="bannerImage"
      alt="Рекламный баннер"
      class="banner-image"
      @error="bannerImage = null"
    />
    <div v-else class="banner-placeholder">
      <span>Рекламный баннер</span>
    </div>
  </div>
</template>
```

### Динамическая замена баннера

Если нужно менять баннер в зависимости от страницы:

```vue
<script setup>
const route = useRoute()

const bannerImage = computed(() => {
  // Разные баннеры для разных страниц
  if (route.path === '/') {
    return '/images/banner-home.jpg'
  } else if (route.path.startsWith('/blog')) {
    return '/images/banner-blog.jpg'
  }
  return '/images/banner.jpg'
})
</script>
```

## 🎨 Стилизация баннера

Текущие стили в `components/TopBanner.vue`:

```css
.top-banner {
  width: 100%;
  max-width: 1200px;
  aspect-ratio: 1920 / 600;  /* Сохраняет пропорции */
  border-radius: var(--radius-base);
  overflow: hidden;          /* Обрезает изображение по скруглению */
}

.banner-image {
  width: 100%;
  height: 100%;
  object-fit: cover;         /* Заполняет весь контейнер */
}
```

### Изменить способ отображения

Если изображение не заполняет весь баннер, измените `object-fit`:

```css
.banner-image {
  object-fit: cover;    /* Заполняет (обрезает при необходимости) */
  /* или */
  object-fit: contain;  /* Вписывает полностью (может быть пустое место) */
  /* или */
  object-fit: fill;     /* Растягивает (может исказить пропорции) */
}
```

## ✅ Быстрая проверка

После размещения изображения:

1. Убедитесь, что файл находится в `public/images/banner.jpg`
2. Запустите проект: `npm run dev`
3. Откройте браузер и проверьте, что баннер отображается
4. Если изображение не видно, проверьте консоль браузера на ошибки 404

## 🔄 Замена баннера в будущем

Просто замените файл `public/images/banner.jpg` на новый файл с тем же именем. 
Изменения применятся сразу (в dev режиме) или после пересборки (в production).

## 💡 Совет

Для оптимизации используйте:
- **WebP** формат для лучшего сжатия
- **Lazy loading** для больших изображений:
  ```vue
  <img loading="lazy" :src="bannerImage" />
  ```
