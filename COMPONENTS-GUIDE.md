# Руководство по компонентам и стилям

## 📁 Где находятся компоненты

Все компоненты находятся в папке **`/components`**:

```
components/
├── AppHeader.vue      # Шапка сайта
├── AppFooter.vue      # Футер
├── ServerCard.vue     # Карточка сервера
├── FiltersPanel.vue   # Панель фильтров
└── TopBanner.vue      # Рекламный баннер
```

## 🎨 Где определены стили

### 1. CSS переменные (глобальные стили)

**Файл:** `layouts/default.vue` (строки 13-51)

Здесь определены все CSS переменные дизайн-системы:

```css
:root {
  /* Цвета */
  --primary-main: #DF2027;
  --primary-hover: #C11B21;
  --primary-contrast: #F5F5F7;
  
  /* Размеры шрифтов */
  --font-xs: 12px;
  --font-sm: 14px;
  --font-base: 16px;
  --font-lg: 18px;
  --font-xl: 20px;
  --font-h2: 32px;
  --font-h1: 48px;
  
  /* Отступы */
  --spacing-xs: 4px;
  --spacing-sm: 8px;
  --spacing-md: 16px;
  --spacing-lg: 24px;
  --spacing-xl: 32px;
  --spacing-xxl: 48px;
  
  /* Радиусы скругления */
  --radius-sm: 4px;
  --radius-base: 8px;
  --radius-lg: 12px;
  --radius-xl: 16px;
  --radius-button: 26px;
  --radius-full: 9999px;
}
```

### 2. Дизайн-система (JSON)

**Файл:** `docs/design-system.json`

Содержит описание компонентов и токенов в JSON формате.

### 3. Стили компонентов

Стили определены **внутри каждого компонента** в секции `<style scoped>`.

## 🔘 Кнопки

### Где определены стили кнопок

Стили кнопок определены в нескольких местах:

1. **`components/AppHeader.vue`** (строки 99-112)
2. **`components/AppFooter.vue`** (строки 80-95)
3. **`components/FiltersPanel.vue`** (строки 221-230)
4. **На страницах** (add-server.vue, advertisement.vue и т.д.)

### Текущие размеры кнопок

```css
.btn-primary {
  padding: 15px 24px;        /* Вертикальный и горизонтальный отступ */
  border-radius: 26px;       /* Скругление углов */
  font-size: 16px;           /* Размер шрифта (var(--font-base)) */
  font-weight: 600;          /* Жирность (var(--font-semibold)) */
}
```

### Как изменить размеры кнопок

#### Вариант 1: Изменить в конкретном компоненте

Откройте файл компонента (например, `components/AppHeader.vue`) и измените стили:

```css
.btn-primary {
  padding: 20px 30px;        /* Увеличить отступы */
  font-size: 18px;           /* Увеличить размер шрифта */
  border-radius: 30px;       /* Изменить скругление */
}
```

#### Вариант 2: Создать глобальный стиль

Добавьте в `layouts/default.vue` в секцию `<style>` (не scoped):

```css
/* Глобальные стили кнопок */
.btn-primary {
  padding: 20px 30px;
  font-size: 18px;
  border-radius: 30px;
}
```

#### Вариант 3: Использовать CSS переменные

Добавьте переменные в `layouts/default.vue`:

```css
:root {
  --button-padding-y: 15px;
  --button-padding-x: 24px;
  --button-font-size: 16px;
  --button-border-radius: 26px;
}
```

Затем используйте в компонентах:

```css
.btn-primary {
  padding: var(--button-padding-y) var(--button-padding-x);
  font-size: var(--button-font-size);
  border-radius: var(--button-border-radius);
}
```

## 📏 Размеры компонентов

### Кнопки

- **Primary кнопка:** `padding: 15px 24px`, `font-size: 16px`
- **Secondary кнопка:** `padding: 15px 24px`, `font-size: 16px`

### Карточки

- **ServerCard:** `padding: var(--spacing-md)` (16px)
- **Tariff cards:** `padding: var(--spacing-xl)` (32px)

### Отступы

- **xs:** 4px
- **sm:** 8px
- **md:** 16px
- **lg:** 24px
- **xl:** 32px
- **xxl:** 48px

## 🎯 Быстрые изменения

### Увеличить размер всех кнопок

1. Откройте `layouts/default.vue`
2. Добавьте в `:root`:
```css
--button-padding-y: 18px;
--button-padding-x: 32px;
--button-font-size: 18px;
```

3. Найдите все `.btn-primary` в компонентах и замените:
```css
padding: var(--button-padding-y) var(--button-padding-x);
font-size: var(--button-font-size);
```

### Изменить цвет кнопок

В `layouts/default.vue` измените:
```css
--primary-main: #DF2027;      /* Основной цвет */
--primary-hover: #C11B21;     /* Цвет при наведении */
```

### Изменить скругление кнопок

В `layouts/default.vue` измените:
```css
--radius-button: 26px;        /* Текущее значение */
```

Или в конкретном компоненте:
```css
.btn-primary {
  border-radius: 30px;         /* Новое значение */
}
```

## 📝 Примеры использования

### Маленькая кнопка
```css
.btn-small {
  padding: 8px 16px;
  font-size: 14px;
  border-radius: 20px;
}
```

### Большая кнопка
```css
.btn-large {
  padding: 20px 40px;
  font-size: 20px;
  border-radius: 30px;
}
```

### Кнопка без скругления
```css
.btn-square {
  border-radius: 0;
}
```

## 🔍 Где искать стили конкретных элементов

- **Кнопки:** `components/AppHeader.vue`, `components/AppFooter.vue`, `components/FiltersPanel.vue`
- **Карточки серверов:** `components/ServerCard.vue`
- **Панель фильтров:** `components/FiltersPanel.vue`
- **Шапка:** `components/AppHeader.vue`
- **Футер:** `components/AppFooter.vue`
- **Глобальные переменные:** `layouts/default.vue`

## 💡 Совет

Для единообразия лучше всего:
1. Определить все размеры кнопок через CSS переменные в `layouts/default.vue`
2. Использовать эти переменные во всех компонентах
3. При необходимости переопределять в конкретных компонентах
